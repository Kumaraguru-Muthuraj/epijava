
------------------
# **Problem: Optimize Water Distribution in a Village**
**Time limit:** 40 minutes
## **Statement**
There are **`n` houses** in a village, and each house must have access to water. You can supply water using **one of two methods**:

### 1️⃣ Build a Well
You may build a well directly at a house.
* The cost of building a well at house `i` is given by `wells[i - 1]`.
* Houses are numbered from `1` to `n`, while the `wells` array is **0-indexed**.

### 2️⃣ Lay Water Pipes
You may connect two houses using a pipe so that water can flow between them.
* Pipe connections are described in the array `pipes`.
* Each entry is of the form `[house1, house2, cost]`, meaning it costs `cost` to build a **two-way** pipe between `house1` and `house2`.
* Multiple pipe options may exist between the same pair of houses, possibly with different costs. You may choose the most cost-effective option.
---
## **Objective**
Determine the **minimum total cost** required to ensure that **every house has access to water**, either by:
* building a well directly at the house, or
* connecting it (directly or indirectly) via pipes to another house that has water.
---
## **Constraints**
* `2 ≤ n ≤ 10⁴`
* `wells.length = n`
* `0 ≤ wells[i] ≤ 10⁵`
* `1 ≤ pipes.length ≤ 10⁴`
* `pipes[j].length = 3`
* `1 ≤ house1j, house2j ≤ n`
* `house1j ≠ house2j`
* `0 ≤ costj ≤ 10⁵`
---
If you want next, I can:

* Explain the **graph + Minimum Spanning Tree (MST)** intuition
* Show how to model wells as a **virtual node**
* Walk through a **sample input step-by-step**
* Provide clean **Java / Python code**

-----------------------------
# **Problem: Continuous Subarray Sum**
## **Statement**
You are given an integer array `nums` and an integer `k`.

Your task is to determine whether the array contains a **good subarray**.
Return `true` if such a subarray exists; otherwise, return `false`.

A subarray of `nums` is considered **good** if:
1. Its length is **at least 2**.
2. The **sum of its elements** is a **multiple of `k`**.

## **Notes**
* A **subarray** is a contiguous sequence of elements within an array.
* An integer `x` is a **multiple of `k`** if there exists an integer `n` such that `x = n × k`.
* **0 is always considered a multiple of `k`.**

## **Constraints**
* `1 ≤ nums.length ≤ 10⁴`
* `0 ≤ nums[i] ≤ 10⁵`
* `0 ≤ sum(nums[i]) ≤ 2³¹ − 1`
* `1 ≤ k ≤ 2³¹ − 1`

If you want next, I can:
* Explain the **prefix sum + modulo** intuition
* Walk through **examples**
* Show the **O(n) hashmap solution**
* Provide clean **Java / Python code**

-----------------------------
# **Problem: Moving Average from Data Stream**
**Time limit:** 15 minutes
## **Statement**
You are given a stream of integers and a window size. Your task is to calculate the **moving average** of all integers within the current sliding window.
Implement a class called `MovingAverage` with the following methods:
* **`MovingAverage(int size)`**
  Initializes the object with the specified window size.
* **`double next(int val)`**
  Adds a new integer `val` to the stream and returns the moving average of the **last `size` values**.

## **Constraints**
* `1 ≤ size ≤ 100`
* `−10³ ≤ val ≤ 10³`
* At most `10²` calls will be made to `next`

If you want, I can also:
* Show a **queue-based implementation**
* Explain the **running sum optimization**
* Provide **Java / Python code**
* Walk through a **sample input-output sequence**

class MovingAverage {
    public MovingAverage(int size) {
    }
    public double next(int val) {
        // Replace this placeholder return statement with your code
        return 0.0;
    }
}
--------------------------------------
# **Problem: Word Search II**
**Time limit:** 40 minutes
## **Statement**
You are given a 2D grid of uppercase English letters and a list of strings `words`.
Your task is to find **all words from the list** that can be constructed in the grid.
A word can be constructed from letters of **sequentially adjacent cells**, where:
* Adjacent cells are neighbors **horizontally or vertically** (not diagonally).
* Each cell may be used **only once per word**.
Return a list containing all the words from the input list that are found in the grid.
The order of the returned words does not matter.
## **Constraints**
* `1 ≤ rows, columns ≤ 12`
* `1 ≤ words.length ≤ 3 × 10³`
* `1 ≤ words[i].length ≤ 10`
* `grid[i][j]` is an uppercase English letter
* `words[i]` consists of uppercase English letters
* All words are **unique**

If you want next, I can:
* Explain the **Trie + DFS optimization**
* Walk through a **sample grid**
* Show **time/space complexity analysis**
* Provide **clean Java / Python code**

-------------------------------------
# **Problem: Perfect Squares**
**Time limit:** 30 minutes
## **Statement**
You are given an integer `n`.
Your task is to return the **minimum number of perfect square numbers** whose sum equals `n`.
A **perfect square** is an integer that is the square of another integer.
For example:
* Perfect squares: `1`, `4`, `9`, `16`, …
* Not perfect squares: `3`, `5`, `11`, …

## **Constraints**
* `1 ≤ n ≤ 10³`

If you’d like, I can also:
* Walk through the **DP solution step by step**
* Explain the **BFS / number-theory approach**
-------------------------------------
# **Problem: Divide Chocolate**
**Time limit:** 40 minutes
## **Statement**
You are given a chocolate bar made up of several chunks, where each chunk has a certain sweetness. The sweetness of each chunk is provided in an array called `sweetness`.
You want to share the chocolate with **`k` friends**. To do this, you will make **`k` cuts**, dividing the chocolate bar into **`k + 1` contiguous parts**.
Each part must consist of **consecutive chunks**.
Since you are a kind person, you will take the piece with the **minimum total sweetness**, and give the remaining pieces to your friends.
Your task is to determine the **maximum possible total sweetness** of the piece you will receive by making the cuts optimally.

## **Constraints**
* `0 ≤ k < sweetness.length ≤ 10³`
* `1 ≤ sweetness[i] ≤ 10³`

If you want, I can next explain:
* The **human intuition**
* The **binary search on answer** approach

-------------------------------------
# **Problem: Nth Magical Number**
**Time limit:** 40 minutes
## **Statement**
You are given three integers `n`, `a`, and `b`.
Your task is to return the **nᵗʰ magical number**.
A **magical number** is defined as a **positive integer** that is divisible by **either `a` or `b`**.
Since the result can be very large, return the answer **modulo**:
[10^9 + 7]

## **Constraints**
* `1 ≤ n ≤ 10^9`
* `2 ≤ a, b ≤ 4 × 10^4`

If you want next, I can explain:
* The **binary search + LCM** solution
* A **human-friendly intuition**
* Full **Java / Python code**
* Step-by-step examples

-------------------------------------
Here is the problem formatted cleanly and interview-ready:
# **Problem: Triangle**
**Time limit:** 30 minutes
## **Statement**
You are given a triangular array `triangle`.
Your task is to return the **minimum path sum** from **top to bottom**.
At each step, you may move to an **adjacent number** in the row below.
More formally, if you are at index `i` in the current row, you may move to:
* Index `i` **or**
* Index `i + 1`
in the next row.

## **Constraints**
* `1 ≤ triangle.length ≤ 200`
* `triangle[0].length = 1`
* `triangle[i].length = triangle[i - 1].length + 1`
* `-10⁴ ≤ triangle[i][j] ≤ 10⁴`
-------------------------------------
# **Problem: Reorder List**
**Time limit:** 30 minutes
## **Statement**
Given the **head** of a singly linked list, reorder the list as if it were **folded onto itself**.
For a list represented as:
```
L0 → L1 → L2 → … → L(n−2) → L(n−1) → Ln
```
Reorder it to:
```
L0 → Ln → L1 → L(n−1) → L2 → L(n−2) → …
```
### Important Notes
* You **must not modify the values** of the nodes.
* Only the **node pointers (links)** may be changed.
* The reordering must be done **in-place**.
---

## **Constraints**
* Number of nodes in the list: `1 ≤ n ≤ 500`
* `-5000 ≤ Node.value ≤ 5000`

If you want, I can also explain the optimal **find-middle → reverse → merge** approach or provide the full implementation.
-------------------------------------
# **Problem: Minimum Moves to Spread Stones Over Grid**
**Time limit:** 30 minutes
## **Statement**
You are given a `3 × 3` grid of integers, where each cell value represents the number of stones in that cell.
Your task is to determine the **minimum number of moves** required to make **every cell contain exactly one stone**.

## **Rules**
* In one move, **only one stone** can be moved.
* A stone may be moved **only to an adjacent cell** (cells sharing a side).
* The **total number of stones** in the grid is exactly **9**.

## **Constraints**
* `grid.length = grid[i].length = 3`
* `0 ≤ grid[i][j] ≤ 9`
* Sum of all values in `grid` equals `9`
-------------------------------------
# **Problem: Word Break II**
**Time limit:** 40 minutes

## **Statement**
You are given a string `s` and an array of strings `wordDict`, representing a dictionary of valid words.
Your task is to insert spaces into `s` such that it is broken into a sequence of valid dictionary words.
Return **all possible sentences** that can be formed using words from `wordDict`.
* The order of the returned sentences does **not** matter.
* A dictionary word may be **reused multiple times**.

## **Constraints**
* `1 ≤ s.length ≤ 20`
* `1 ≤ wordDict.length ≤ 1000`
* `1 ≤ wordDict[i].length ≤ 10`
* `s` and all `wordDict[i]` consist only of **lowercase English letters**
* All strings in `wordDict` are **unique**

If you want, I can also provide the DFS + memoization solution or walk through an example step by step.
-------------------------------------

# **Problem: Candy**
**Time limit:** 40 minutes
## **Statement**
You are given an array `ratings`, where `ratings[i]` represents the rating of the *i-th* child standing in a line.
Your task is to distribute candies to the children according to the following rules:
1. Every child must receive **at least one candy**.
2. A child with a **higher rating** than an immediate neighbor must receive **more candies** than that neighbor.

Return the **minimum total number of candies** required to satisfy these conditions.

## **Constraints**
* `1 ≤ ratings.length ≤ 1000`
* `0 ≤ ratings[i] ≤ 1000`

“I start by giving everyone one candy.
Then I do a left-to-right pass to satisfy left neighbors,
followed by a right-to-left pass to satisfy right neighbors,
taking the maximum to preserve both constraints.”

-------------------------------------
# **Problem: Max Consecutive Ones**
**Time limit:** 15 minutes
## **Statement**
You are given a binary array `nums` (containing only `0`s and `1`s).
Your task is to find the **maximum number of consecutive `1`s** in the array and return that value.

## **Constraints**
* `1 ≤ nums.length ≤ 10³`
* `nums[i] ∈ {0, 1}`

## **Notes / Dive Deep**
* This can be solved in a single pass using a running counter.
* Aim for **O(n)** time and **O(1)** extra space.
-------------------------------------

# **Problem: Rectangle Area** - EPI Chapter 1 last problem.
**Time limit:** 30 minutes
## **Statement**
You are given the coordinates of **two axis-aligned rectangles** on a 2D plane.
Your task is to compute the **total area covered by both rectangles**.

* The **first rectangle** is defined by:
  * Bottom-left corner: `(ax1, ay1)`
  * Top-right corner: `(ax2, ay2)`
* The **second rectangle** is defined by:
  * Bottom-left corner: `(bx1, by1)`
  * Top-right corner: `(bx2, by2)`

**Note:**
The rectangles may **overlap**, and any overlapping area should be counted **only once**.

## **Constraints**
* `-10⁴ ≤ ax1 ≤ ax2 ≤ 10⁴`
* `-10⁴ ≤ ay1 ≤ ay2 ≤ 10⁴`
* `-10⁴ ≤ bx1 ≤ bx2 ≤ 10⁴`
* `-10⁴ ≤ by1 ≤ by2 ≤ 10⁴`
-------------------------------------
# **Problem: Maximum Value at a Given Index in a Bounded Array**

**Time limit:** 30 minutes

## **Statement**

You are given three positive integers `n`, `index`, and `maxSum`.
Construct an array `nums` of length `n` such that all of the following conditions are satisfied:

1. The length of the array `nums` is exactly `n`.
2. Each element `nums[i]` is a **positive integer**.
3. The absolute difference between any two consecutive elements is **at most 1**:
   `|nums[i] − nums[i + 1]| ≤ 1`
4. The **sum of all elements** in `nums` does **not exceed `maxSum`**.
5. The value at position `index`, `nums[index]`, is **maximized**.

Your task is to **return the maximum possible value of `nums[index]`**.

## **Constraints**
* `1 ≤ n ≤ maxSum ≤ 10⁹`
* `0 ≤ index < n`

If you want, I can also provide the intuition and optimal binary-search solution for this problem.


-------------------------------------
# **Problem: Middle of the Linked List**
**Time limit:** 15 minutes
## **Statement**
Given the **head** of a singly linked list, return the **middle node** of the list.
* If the list has an **odd** number of nodes, return the single middle node.
* If the list has an **even** number of nodes, there are two middle nodes — **return the second one**.

Return the node itself (not just its value).

## **Constraints**
* Let `n` be the number of nodes in the list.
* `1 ≤ n ≤ 100`
* `1 ≤ Node.value ≤ 100`
* `head ≠ NULL` (the list is non-empty)

## **Hints / Dive Deep**
* Use the **two-pointer (slow & fast)** technique to find the middle in **O(n)** time and **O(1)** space.
* Consider why returning the second middle for even-length lists works naturally with the usual slow/fast setup (fast moves 2 steps, slow moves 1).
-------------------------------------
Problem: Contains Duplicate II
Time Limit: 15 minutes
Statement:
You are given an integer array nums and an integer k. Determine whether there exist two distinct indices i and j in the array such that:
nums[i] == nums[j], and
|i - j| ≤ k
Return TRUE if such indices exist; otherwise, return FALSE.

Constraints:
1 ≤ nums.length ≤ 10³
−10³ ≤ nums[i] ≤ 10³
0 ≤ k ≤ 10⁴

-------------------------------------
# Problem: Lucky Numbers in a Matrix
**Time limit:** 15 minutes
## Statement
Given an `m × n` matrix of **distinct** numbers, return the lucky number(s) in the matrix.
A **lucky number** is an element that is the **smallest in its row** and **largest in its column**.
Return all lucky numbers in any order.
---
## Definitions
* `m = matrix.length`
* `n = matrix[i].length`
---
## Constraints
* `1 ≤ m, n ≤ 50`
* `1 ≤ matrix[i][j] ≤ 10^5`
* All elements in the matrix are distinct.
## Follow-ups / Dive Deep (optional)
* Can you find all lucky numbers in **O(m·n)** time and **O(m + n)** extra space?
* How would your approach change if elements were **not distinct**?

-------------------------------------
# Problem: Number of Islands

**Time limit:** 30 minutes

## Statement

Given an `m × n` 2D grid of binary characters where `'0'` represents **water** and `'1'` represents **land**,
an **island** is a group of `'1'` cells connected **horizontally or vertically** (not diagonally).
Return the total number of islands in the grid.

## Input definition

* `grid.length = m`
* `grid[i].length = n`
* `grid[i][j]` is a character `'0'` or `'1'`.

## Constraints

* `1 ≤ m ≤ 50`
* `1 ≤ n ≤ 50`
* `grid[i][j] ∈ {'0', '1'}`

## Notes / Dive Deep (optional)

* Typical solutions use DFS, BFS, or Union-Find.
* Aim for **O(m·n)** time and **O(m·n)** worst-case auxiliary space (stack/queue or UF).
* Consider in-place marking of visited cells (e.g., flip visited `'1'` → `'0'`) to avoid extra memory.
public static int numIslands(List<List<Character>> grid) {

    // Replace this placeholder return statement with your code
    return -1;
}
-------------------------------------
# **Problem: Longest Common Prefix**

**Time limit:** 15 minutes

## **Statement**

Given an array of strings `strs`, write a function to find the **longest common prefix** among them.
If no common prefix exists, return an empty string `""`.

## **Constraints**

* `1 ≤ strs.length ≤ 200`
* `0 ≤ strs[i].length ≤ 200`
* Each `strs[i]` consists only of **lowercase English letters**.
public static String longestCommonPrefix(String[] strs) {

    // Replace the following return statement with your code
    return "";
}
-------------------------------------
# **Problem: Implement Trie (Prefix Tree)**

**Time limit:** 30 minutes

## **Statement**

A **Trie** (or *prefix tree*) is a tree-like data structure used to store strings, enabling efficient prefix-based lookups.
Your task is to **implement a Trie** supporting the following operations:

* **insert(word):** Insert a word into the Trie.
* **search(word):** Return **TRUE** if the exact word exists in the Trie; otherwise, return **FALSE**.
* **startsWith(prefix):** Return **TRUE** if any word in the Trie starts with the given prefix; otherwise, return **FALSE**.

## **Constraints**

* `1 ≤ word.length, prefix.length ≤ 500`
* All strings consist only of **lowercase English letters**.
* At most `10³` function calls will be made in total.

If you'd like, I can generate the full Trie implementation in Java, Python, or C++.
class Trie {

    public Trie() {
        // Write your code here
    }

    // inserting string in trie
    public void insert(String word) {
        // Write your code here
    }

    // searching for a string
    public boolean search(String word) {
        // Replace this placeholder return statement with your code
        return false;
    }

    // searching for a prefix
    public boolean searchPrefix(String prefix) {
        // Replace this placeholder return statement with your code
        return false;
    }
}
----------------------------------------
# **Problem: Parsing a Boolean Expression**
**Time limit:** 40 minutes
## **Statement**

You are given a string `expression` that represents a boolean expression. The expression may take any of the following forms:

* **`'t'`** — represents boolean **TRUE**
* **`'f'`** — represents boolean **FALSE**
* **`!(expr)`** — logical **NOT** applied to subexpression `expr`
* **`&(expr1, expr2, ..., exprN)`** — logical **AND** over one or more subexpressions; returns TRUE only if **all** subexpressions evaluate to TRUE
* **`|(expr1, expr2, ..., exprN)`** — logical **OR** over one or more subexpressions; returns TRUE if **at least one** subexpression is TRUE

Your task is to **parse the given expression** and return its **boolean evaluation result**.

**Note:**
The input is guaranteed to be valid and follow the described syntax exactly.

## **Constraints**
* `1 ≤ expression.length ≤ 20,000`
* Each character of `expression` is one of:
  `(`, `)`, `&`, `|`, `!`, `t`, `f`, `,`

If you want, I can also provide:
✅ DFS parsing approach
✅ Stack-based solution
✅ Complete Java/Python implementation
Just ask!

public boolean parseBoolExpr(String expression) {

    // Replace this placeholder return statement with your code
    return false;
}

Examples:
!(!(t)) ---> true
&(t, |(f,f), !(f)) -> false
|(&(t,t),&(f,t)) -> true
----------------------------------------

# **Problem: Find the Lexicographically Largest String From Box II**
**Time limit:** 40 minutes
## **Statement**
You are given:
* A string `word`
* An integer `numFriends`, representing the number of friends in a game organized by Alice.
The game proceeds in **multiple rounds**, and in each round:

1. The string `word` is split into **exactly `numFriends` non-empty substrings**.
2. The split must be **unique** — no previous round has produced the same sequence of substrings.
3. All substrings produced in the round are placed into a **box**.

After **all possible unique splits** have been performed, your task is to determine the **lexicographically largest string** among **all substrings** placed into the box.

## **Lexicographical Order Definition**
A string **`a`** is lexicographically larger than string **`b`** if:
* At the first position where they differ, `a` has a character that comes **later** in the alphabet than the corresponding character in `b`, **or**
* If `a` is a **prefix** of `b`, then the **longer** string is considered larger.

## **Constraints**
* `1 ≤ word.length ≤ 10³`
* `word` consists only of lowercase English letters.
* `1 ≤ numFriends ≤ word.length`

If you'd like, I can also generate a solution outline, optimal approach, or full code.
public String answerString(String word, int numFriends)
{
    // Replace this placeholder return statement with your code
    return "";
}
---------------------------------
# **Problem: Remove Invalid Parentheses**

**Time limit:** 40 minutes

## **Statement**

You are given a string `s` containing:

* Lowercase English letters
* Opening `'('` and closing `')'` parentheses

A string is considered **valid** if:

1. Every opening `'('` has a matching closing `')'`.
2. Parentheses are correctly ordered and properly nested.
3. Letters can appear anywhere and do **not** affect validity.

Your task is to **return all possible valid strings** that can be formed by removing the **minimum number** of invalid parentheses.
The output must be a **list of unique strings**, in any order.

## **Constraints**
* `1 ≤ s.length ≤ 25`
* `s` consists of lowercase English letters and the characters `'('` and `')'`
* The string contains **at most 20 parentheses**

If you want, I can also provide the optimal BFS or DFS backtracking solution.
---------------------------------
# **Problem: Inorder Successor in BST**

**Time limit:** 30 minutes

## **Statement**

You are given the **root** of a Binary Search Tree (BST) and a specific **node `p`**.
Your task is to return the **inorder successor** of node `p`.

If no inorder successor exists, return **NULL**.

**Definition:**
The inorder successor of `p` is the node with the **smallest value greater than `p.data`** in the BST.

## **Constraints**
* The tree contains **1 to 500 nodes**.
* `-10⁴ ≤ Node.data ≤ 10⁴`
* All node values are **unique**.
* The node `p` is guaranteed to exist in the tree.

If you want, I can also provide the optimal O(h)-time solution using BST properties.
---------------------------------
